# Current Task

## Current Parent Task
Form Components (Phase 1)

## Current Subtask
U1.1.3 Select Dropdown

## Status
Pending Approval

## Goal
Build a standard select input component that integrates with the `<x-form.wrapper>`.

## Dependencies
**Depends On:**
- U1.1.1 Form Wrapper (Completed)

**Blocks:**
- U1.1.4 SearchBox

## Required Deliverables
- A Blade component (`<x-form.select>`) that handles single select dropdowns.
- Must use `<x-form.wrapper>` internally to render label, hint, and errors.
- Support options array or slot based options.
- Support `wire:model` and default attributes.
- Automatically wire `aria-describedby` when hint or error is present.

## Completed Prerequisites
- U0.5 Layout Templates
- U1.1.1 Form Wrapper
- U1.1.2 Input Field
